# ============================================================
#  必填 —— 只有这两个
# ============================================================

# 1) 数据库。Neon 控制台 → Connection string → 勾 "Pooled connection"
DATABASE_URL="postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# 2) 管理员密码。首次启动自动创建管理员账户；改这个值并重启即可改密码。
ADMIN_PASSWORD=""


# ============================================================
#  以下全部可选，不填就用默认值
# ============================================================

# 管理员登录邮箱，默认 admin@localhost
# ADMIN_EMAIL="admin@example.com"

# 凭据加密密钥（AES-256-GCM，用于加密各节点的 api_secret 和 WHIP stream key）。
# 不填：首次启动自动生成一把并存进数据库 —— 零配置可用。
# 填了：密钥待在数据库之外，隔离性更强，生产环境建议填。
#   生成：npm run keygen
#   注意：一旦填了就不能再改，换了以后已存的节点凭据全部解不开。
# CREDENTIAL_ENCRYPTION_KEY=""

# 站点地址，用于拼 LiveKit webhook 回调地址。
# 不填会从请求头推导 —— Vercel preview 部署上反而更准
# （NEXT_PUBLIC_* 是构建期内联的，写死会导致 preview 域名对不上）。
# NEXT_PUBLIC_APP_URL="http://localhost:3000"

# 保护 /api/cron/cleanup，防止被公网随意触发。部署到 Vercel 时建议设。
# CRON_SECRET=""


# ============================================================
#  LiveKit 不需要环境变量
# ============================================================
# 节点在网页里配：登录后到控制台「+ 接入我的节点」填 URL / API Key / Secret，
# 保存前会实地打一次 LiveKit API 验证。想让所有用户共享某个节点，
# 到 /admin 把它「设为内置」。
